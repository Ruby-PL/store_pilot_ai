#!/usr/bin/env ruby
require_relative "load_env"

port = ENV.fetch("PORT", "3000")

env = {
  "SOLID_QUEUE_IN_PUMA" => ENV.fetch("SOLID_QUEUE_IN_PUMA", "true")
}

exec env, "./bin/rails", "server", "--port", port, *ARGV
